export type LimitsType = {
  title: string;
  amount: string;
  value: string;
  growthColor: string;
};

export type RecentTransactionType = {
  title: string;
  status: string;
  time: string;
  transaction: string;
  amount: string;
  addition: string;
};
